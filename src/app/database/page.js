import DatabaseDashboard from "@/components/DatabaseDashboard";

export const metadata = {
  title: "MTG Database | Playtest Forge",
};

export default function DatabasePage() {
  return <DatabaseDashboard />;
}
